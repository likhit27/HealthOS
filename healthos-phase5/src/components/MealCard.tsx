import {Check,RefreshCw} from "lucide-react";import {PlannedMeal} from "@/lib/types";
export default function MealCard({meal,done,onDone,onSwap}:{meal:PlannedMeal;done:boolean;onDone:()=>void;onSwap:()=>void}){return <div className="rounded-2xl border border-[var(--border)] p-4"><div className="flex justify-between gap-3"><div><div className="text-[10px] font-black uppercase tracking-wider text-[var(--muted)]">{meal.slot}</div><b className="mt-1 block">{meal.name}</b></div><button onClick={onDone} className={`flex h-9 w-9 items-center justify-center rounded-full ${done?'bg-[var(--accent)]':'bg-gray-100'}`}><Check size={17}/></button></div><div className="mt-2 text-xs text-[var(--muted)]">{meal.calories} kcal · {meal.protein}g protein · {meal.carbs}g carbs · {meal.fat}g fat · {meal.fibre}g fibre</div><div className="mt-3 flex justify-between"><span className="pill">{meal.gutRisk==='low'?'✓ Gut-friendly':'~ Moderate'}</span><button onClick={onSwap} className="flex items-center gap-1 text-xs font-bold text-[var(--primary)]"><RefreshCw size={13}/> Change</button></div></div>}
