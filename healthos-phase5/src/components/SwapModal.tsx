import {Meal,MealType} from "@/lib/types";
export default function SwapModal({day,slot,options,onSelect,onClose}:{day:string;slot:MealType;options:Meal[];onSelect:(m:Meal)=>void;onClose:()=>void}){return <div className="fixed inset-0 z-50 flex items-end justify-center bg-black/30 p-4 md:items-center"><div className="max-h-[85vh] w-full max-w-xl overflow-y-auto rounded-3xl bg-white p-6"><div className="flex justify-between"><div><div className="text-xs font-bold uppercase text-[var(--muted)]">{day} · {slot}</div><h2 className="text-2xl font-black">Choose alternative</h2></div><button onClick={onClose}>✕</button></div><div className="mt-5 space-y-2">{options.map(m=><button key={m.id} onClick={()=>onSelect(m)} className="w-full rounded-2xl border border-[var(--border)] p-4 text-left hover:bg-[var(--surface2)]"><div className="flex justify-between gap-3"><b>{m.name}</b><span className="pill">{m.protein}g protein</span></div><div className="mt-1 text-xs text-[var(--muted)]">{m.calories} kcal · {m.carbs}g carbs · {m.fat}g fat · {m.fibre}g fibre</div></button>)}</div></div></div>}
